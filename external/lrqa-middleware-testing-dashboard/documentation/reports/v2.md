# v2.0 Requirements & Architecture

## 1. Repository Structure & Code Organization
Create a new repository based on the current v1.0 codebase and refactor it as v2.0. Implement a strict Model-View-Controller (MVC) architecture. All user input forms should be implemented as modal pop-ups, organized in separate folders with names that combine the folder purpose with related method names (e.g., `modals/device_management_device_add.html`).

## 2. Memory & Change Tracking System
Establish a comprehensive memory file for v2.0 that maintains a complete audit trail. This memory should continuously update with: all implemented changes, detailed descriptions of each modification, comprehensive application information, architectural decisions, and version history. The memory serves as the single source of truth for v2.0 evolution.

## 3. AI Agents & Skills Framework
Develop a modular AI agent system consisting of:
- Multiple specialized sub-agents, each assigned specific monitoring tasks
- A parent orchestrator agent that oversees all sub-agents
- Real-time communication between child agents and parent agent
- Continuous task monitoring and status updates
- All agent activities logged to the shared memory file for transparency and auditability

## 3.1. Agent-MemoryMonitor: v2.0 Implementation Progress Tracker (AI Sub-Agent)
Implement a dedicated AI sub-agent to maintain comprehensive tracking and monitoring of the v2.0 implementation:
- **Memory System Architecture**:
  - `/memories/repo/v2-requirements-summary.md` - All 18 requirements detailed with status
  - `/memories/repo/v2-work-progress.md` - Daily work tracking and session logs
  - `/memories/repo/v2-implementation-status.md` - Phase completion status and deliverables
  - `/memories/repo/v2-blockers-and-issues.md` - Issues, solutions, decisions, and lessons learned
  - `/memories/repo/v2-agent-health.md` - Sub-agent operational status and metrics
- **Agent Responsibilities**:
  - Monitor all memory files for updates and consistency
  - Maintain current implementation status and completion percentages
  - Track completed vs pending tasks across all 5 phases
  - Alert developers to blockers, critical issues, or missed deadlines
  - Generate daily progress reports with metrics
  - Ensure memory files are kept up-to-date with every work session
  - Validate data consistency across memory files
  - Provide quick status snapshots for stakeholders
  - Track architectural decisions and implementation rationale
  - Maintain lessons learned and best practices discovered
- **Integration Points**:
  - Memory updates triggered at end of each work session
  - Daily automated reports sent to team
  - Real-time alerts for critical blockers
  - Weekly progress dashboard with metrics
  - Integration with orchestrator agent for priority adjustments
- **Success Metrics**:
  - 100% accuracy of tracked tasks
  - Memory files updated daily
  - No missed deadline alerts
  - Complete audit trail of all changes
  - Clear visibility of v2.0 completion (target: 100% by Day 45)

## 4. Centralized Database Architecture
Create a global centralized database accessible from any location worldwide. Security requirements include:
- Robust deletion prevention mechanisms with comprehensive retrieval system
- Support for location-based device data organization
- Log line validation entries
- Saved sequences management and version history
- Complete CRUD operations with audit trails
- Multi-level backup and disaster recovery protocols

## 5. Execution Data Persistence & Reliability
Ensure zero loss of execution data—a critical requirement given historical issues in v1.0. All execution records must be:
- Permanently persisted with no corruption risk
- Protected against accidental deletion or modification
- Fully recoverable in any scenario
- Maintained across application restarts and system failures

## 6. Dynamic ETA & Device Lock Management (AI Sub-Agent)
Create a dedicated AI sub-agent to intelligently manage ETA calculations and device lock expiration. Functionality includes:
- Analyzing the active job to determine total iterations queued
- Calculating actual execution time per iteration based on selected methods
- Dynamically adjusting device lock expiration times based on realistic job duration
- Preventing premature lock expiration (resolved v1.0 issue: locks expired mid-execution with multiple iterations)

## 7. Intelligent Email Reporting (AI Sub-Agent)
Implement an AI-powered email agent that generates contextual execution reports:
- Automatically sends emails upon execution completion
- Generates success reports for fully completed executions without failures
- Generates detailed failure reports for interrupted or partially completed executions
- Provides comprehensive execution summaries including status, duration, method breakdown, and error details
- Eliminates the need for users to manually review execution logs to understand outcomes

## 8. Execution Context Preservation
At execution initiation, capture and store all relevant context data for future reference:
- Device specifications and credentials
- Method definitions and parameters
- System configuration at execution time
- Environmental variables and settings
- User and team information
- This prevents post-recovery scenarios (common in v1.0) where executions show as "unknown device," "unknown method," etc.
- Creates an immutable snapshot for complete traceability

## 9. Execution History & Data Retention
Implement robust execution history management with:
- Minimum 30-day retention period for all execution records
- Sophisticated data structures preventing corruption
- Guaranteed data integrity across all scenarios
- Complete audit trail for every execution
- Industry-standard backup and versioning

## 10. Method Documentation & Intent Tracking
Enhance the Methods model with an "intent" or "rationale" field where users can:
- Document why a specific method was selected for execution
- Add contextual notes and test goals
- When saving as a sequence, preserve all method rationale notes
- Enable future users to understand the testing strategy and methodology

## 11. Staging & Approval Workflow for Data Changes
Implement a two-tier data persistence system:
- **Temporary Database**: All changes (new sequences, device updates, log patterns, user registrations) immediately saved here
- **Approval Process**: Admin review and confirmation required for permanent promotion
- **Permanent Database**: Confirmed changes committed with approval timestamp and reviewer information

## 12. Team-Based Real-Time Synchronization
Ensure cross-location consistency across all running instances:
- On application startup, perform a full sync check of: devices, log patterns, system commands, saved sequences, and available methods
- Detect any new items added by other team members in other locations
- Automatically reflect changes across all instances where the application is running within that team
- Enable true collaborative device management across geographic regions

## 13. Database Metadata & Location Tracking
The centralized database must capture comprehensive metadata for all stored information:
- Record the team name for each data entry
- Track the source location where data originated
- Maintain geographic tagging for device groupings
- Enable location-based filtering and queries
- Support multi-team and multi-region deployments with complete data lineage

## 14. Safe Change Management & Impact Analysis
Implement a code-driven, safety-first change deployment system:
- Before applying any changes to the codebase, perform comprehensive full-project impact analysis
- Automatically scan for potential data loss, corruption risks, or application crash scenarios
- Generate a detailed impact report showing all affected components, dependencies, and risks
- Allow changes only if they are non-breaking and do not negatively impact existing functionality
- Maintain a rollback mechanism for all applied changes
- Log all change deployments with their impact assessments for audit purposes

## 15. AI-Powered Screen State Verification (AI Sub-Agent)
Develop an intelligent AI method to verify the current device screen state and identify running applications:
- Capture the current device screen using VNC methodology (from v1.0 infrastructure)
- Analyze the screenshot using Claude Vision API to identify which application is currently displayed
- Compare captured screenshot against reference screenshots in `/reference_screens/` directory for accurate identification
- Return detailed information: current app name, screen state, UI elements detected
- Support optional "expected screen/input" parameter: if provided, verify whether the device landed on the expected screen
- If expectation fails, return mismatch details and suggestions for remediation
- Use reference screenshot library as training data to continuously improve recognition accuracy
- Enable real-time screen state validation without manual inspection

## 16. Distributed Data Synchronization via Docker (AI Sub-Agent)
Implement a distributed data synchronization system that allows Docker instances running in remote locations to push new and modified data to the centralized database and repository:
- **Data Types Managed**: Devices, log patterns, system commands, saved sequences, available methods
- **Bidirectional Sync**: Docker instances can pull latest data from DB and push new/updated data back
- **AI Sub-Agent (DistributedDataSync)**: Monitors all Docker instances, validates data integrity, handles conflicts
- **Conflict Resolution**: Implement versioning and merge strategies (timestamp-based, admin approval for conflicts)
- **Repository Sync**: When data is confirmed in DB, automatically trigger repo update via GitHub API
- **Encryption**: Code and sensitive data encrypted in Docker images (no shared credentials exposed)
- **Audit Trail**: Track every data push: source location, timestamp, agent approval, change details
- **Validation Before Push**: Sub-agent validates schema compliance, data integrity, and no breaking changes before DB/repo commit
- **Fallback Mechanism**: If centralized DB is unreachable, Docker instances maintain local queue and auto-retry with exponential backoff
- **Location-Aware**: Each Docker instance tagged with location/region; data changes include location metadata

## 17. Repository & Code Protection Strategy
Ensure code security and prevent unauthorized sharing:
- **Encrypted Docker Images**: All Python code obfuscated/encrypted (use PyArmor or similar)
- **Proprietary License**: Add Apache 2.0 + commercial license restriction to repo
- **Secrets Management**: No credentials, API keys, or sensitive data in Docker images (pulled from vault at runtime)
- **Immutable Releases**: Main branch tagged versions signed and immutable
- **Access Control**: GitHub repo private; only authorized team members have access
- **Docker Registry**: Push images to private registry (Docker Hub private or self-hosted)
- **Image Verification**: Each Docker image signed with GPG key; verification required before deployment
- **Version Pinning**: All deployments reference specific image SHA, not floating tags

## 18. Multi-Location Deployment Pipeline
Create a scalable deployment infrastructure supporting multiple geographic locations:
- **Deployment Targets**: UK primary, India locations (north/south), potential future regions
- **Docker Image Registry**: Private registry accessible from all locations
- **Deployment Automation**: CI/CD pipeline (GitHub Actions) to build images from Main branch
- **Location-Specific Config**: Each Docker instance receives location-based ENV variables at startup
- **Health Monitoring**: Each deployed instance reports health to central monitoring dashboard
- **Auto-Update Capability**: Ability to deploy new image versions to locations without manual SSH
- **Graceful Migration**: Blue-green deployment strategy to prevent downtime during updates
- **Staged Rollout**: New versions first deployed to test location, then staging locations, then production
- **Rollback Capability**: Ability to rollback to previous image version if issues detected

## 19. AI Sequence Builder Specification (v2.0)
Define and standardize the AI Sequence Builder workflow so sequence creation is explainable, reusable, and team-shareable.

- **Primary Goal**: Convert user-selected execution queues into reusable saved sequences with rationale metadata, team visibility controls, and edit support.
- **API Surface (Modal API)**:
  - `POST /api/sequences/create`
    - Required: `sequence_name`, `rationale`
    - Optional: `description`, `methods`, `device_ids`, `visibility`, `is_readonly`, `tags`
    - Response: `success`, `sequence_id`, `sequence_name`, UTC `timestamp`
  - `GET /api/sequences/<sequence_id>`
    - Returns the normalized sequence payload from the persistence model
  - `PUT /api/sequences/<sequence_id>`
    - Supports both payload styles (`name` or `sequence_name`) and queue/method updates
    - Fields: `name|sequence_name`, `methods`, `user_inputs`, `queue_data`
- **Validation Requirements**:
  - `sequence_name` minimum length 3, allowed chars `[a-zA-Z0-9_-]`
  - `rationale` minimum length 20 characters (UI validation)
  - Backend enforces required fields and returns structured errors (`success: false`, UTC `timestamp`)
- **UI/UX Requirements (Sequence Management Modal)**:
  - Identification: sequence name and description
  - Strategy: mandatory rationale explaining test intent and expected outcomes
  - Configuration: included devices, selected methods, iteration count, estimated duration
  - Sharing: visibility (`private|team|public`) and read-only toggle
  - Metadata: comma-separated tags
  - Edit mode pre-populates existing values and updates in-place
- **Persistence Contract**:
  - Canonical model: `SavedSequence`
  - Key fields: `sequence_id`, `name`, `description`, `queue_data`, `methods`, `user_inputs`, `created_at`, `created_by`, `team_name`, `method_rationale`, `execution_count`, `total_duration_seconds`, `average_duration_seconds`, `location`, `is_active`, `updated_at`
  - Storage strategy: database-first with JSON mirror backup sync for recovery
  - Backward compatibility: supports both legacy `methods/user_inputs` and current `queue_data`
- **Execution Integration**:
  - Queue/job creation supports `sequence_name` and queue payload forwarding
  - Results pipeline consolidates multi-method sequence runs into grouped sequence cards and preserves display-friendly sequence names
- **Auditability & Time Standard**:
  - All API responses and sequence lifecycle logs use UTC timestamps
  - Sequence create/update operations emit explicit success/error logs for traceability

---

## 20. Device Selection Auto-Refresh (Phase 1 - Quick Win)
Automatically refresh the "Select Device(s)" section whenever devices are added or edited.

**Status**: 🔶 PHASE 1 (Implementation Started - July 19, 2026)

**Requirements**:
- Auto-trigger device list refresh when device management modal closes
- Show new/updated devices immediately without page reload
- Maintain selected devices in form (don't clear selection on refresh)
- Support both device add and device edit operations
- Provide visual feedback (loading indicator) during refresh

**Implementation Details**:
- Function `refreshDeviceListFromBackend()` already exists (templates/index.html line 3594) but not auto-triggered
- Add modal close event handlers to trigger auto-refresh
- Update device list via `/api/devices` endpoint
- Preserve device selections in localStorage
- Show refresh status in UI

**Deliverables**:
- [ ] Auto-trigger on device add modal close
- [ ] Auto-trigger on device edit modal close
- [ ] Preserve selected devices after refresh
- [ ] Add loading indicator during refresh
- [ ] Test on both add and edit operations

**Success Metrics**:
- New devices appear in selection immediately without page reload
- Edited device properties update in real-time
- User experience: refresh completes in < 2 seconds
- No loss of user selections during refresh

---

## 21. Device Execution Status Tracking with Username (Phase 1 - Quick Win)
Display devices under execution with BUSY tag and username who triggered it, while allowing queue for next execution.

**Status**: 🔶 PHASE 1 (Implementation Started - July 19, 2026)

**Requirements**:
- Track which user triggered each device execution (executing_user field)
- Display BUSY badge with username on device selection UI
- Allow device to remain selectable for next execution (queue jobs)
- Show queue position for pending jobs on busy device
- Prevent duplicate simultaneous executions on same device
- Store execution metadata for audit trails

**Implementation Details**:
- Add `executing_user` field to Job/ExecutionContext model
- Update queue_service.py to track executing_user
- Modify device_controller.py to return execution status with user info
- Update device selection UI to show BUSY badge
- Allow job queueing even when device is busy (PENDING status)

**Data Model**:
```
Job {
  job_id: str
  device_ip: str
  executing_user: str (NTID)
  execution_status: RUNNING | PENDING | QUEUED | COMPLETED
  triggered_at: datetime
  queued_at: datetime
  queue_position: int (if pending)
}
```

**UI Changes**:
- Show badge: `<span class="badge bg-warning">BUSY - User: john_doe</span>`
- Display queue position: `Queue Position: 2 of 5`
- Device remains selectable
- New jobs automatically added to PENDING queue

**Deliverables**:
- [ ] Add executing_user to Job model
- [ ] Update queue_service to track user
- [ ] Update device controller to return user info
- [ ] Add BUSY badge to device selection UI
- [ ] Implement queuing while device is busy
- [ ] Add queue position display
- [ ] Update test coverage

**Success Metrics**:
- BUSY status visible with correct username
- Queue management works for multiple jobs
- Users can queue jobs on busy devices
- Execution audit trail complete and accurate

---

## 22. Custom Log Filtering for Reboot Performance v2 (Phase 1 - Quick Win)
Allow users to add custom grep commands for log filtering during Reboot Performance v2 execution.

**Status**: 🔶 PHASE 1 (Implementation Started - July 19, 2026)

**Requirements**:
- Provide modal input box for custom grep commands
- Support contains and not_contains conditional logic
- Allow multiple custom log patterns
- Execute custom patterns after standard crash detection
- Collect and display results alongside standard logs
- Support regex patterns for advanced filtering

**Implementation Details**:
- Add modal form before Reboot v2 execution
- Input fields:
  - Log file path (default: `/opt/logs/`)
  - Grep pattern (text input)
  - Match type: "Contains" | "Does NOT contain" (checkboxes)
  - Add/remove buttons for multiple patterns
- Pass customLogs to execution method
- Update method_reboot_perf_v2_optimized.py to process custom patterns
- Append custom grep results to execution log

**UI Modal Structure**:
```
Custom Log Filtering (Optional)
[ ] Add custom grep patterns
  Log File: [/opt/logs/________]
  Pattern:  [________] [Contains ☑] [NOT Contains ☐]
  [+ Add Pattern] [- Remove]
  
[Cancel] [Apply Patterns]
```

**Grep Command Logic**:
- Contains: `grep -E 'pattern' /opt/logs/`
- NOT Contains: `grep -v -E 'pattern' /opt/logs/`
- Multiple: Chain with pipes or separate queries

**Deliverables**:
- [ ] Create custom log filtering modal
- [ ] Add modal to Reboot v2 method execution flow
- [ ] Update method_reboot_perf_v2_optimized.py to accept custom patterns
- [ ] Implement contains/not_contains logic
- [ ] Execute custom grep on device
- [ ] Display results in execution log
- [ ] Test with various regex patterns

**Success Metrics**:
- Users can input custom grep patterns
- Patterns execute correctly (contains vs not_contains)
- Results displayed in execution log
- No performance impact on standard execution

---

## 23. Log Storage Path Optimization to Lexar USB (Phase 1 - Quick Win)
Store device logs in `/media/lrqa/Lexar/` with proper execution folder structure instead of `/media/apps/`.

**Status**: 🔶 PHASE 1 (Implementation Started - July 19, 2026)

**Requirements**:
- Use Lexar USB for log storage (consistent with screenshots)
- Create execution folder structure with job_id organization
- Store tar.gz files under Lexar path, not `/media/apps/`
- Maintain backward compatibility with existing logs
- Support multiple team locations with proper organization
- Ensure device has write permissions to Lexar path

**Implementation Details**:
- Function `get_lexar_base_path()` already exists (method_utils.py)
- Update `capture_device_logs_sftp()` to use Lexar path instead of `/media/apps/`
- Create folder structure:
  ```
  /media/lrqa/Lexar/Enhancement_output/
  ├── EXECUTION_LOGS/
  │   └── {job_id}/
  │       ├── {device_ip}/
  │       │   └── {timestamp}.tgz
  │       └── {device_ip}/
  │           └── {timestamp}.tgz (multiple devices per job)
  └── SCREENSHOTS/
  ```

**Current Code Issue** (method_utils.py line 520):
```python
# WRONG:
remote_log_path = f"/media/apps/{log_filename}"

# CORRECT:
lexar_base = get_lexar_base_path()
remote_log_path = os.path.join(lexar_base, 'EXECUTION_LOGS', job_id, device_ip, log_filename)
```

**Device Permissions**:
- Ensure `/media/lrqa/Lexar/` is writable by device user (10022@xxx.xxx.xxx.xxx)
- Pre-create Lexar folders if they don't exist
- Fall back to `/media/apps/` if Lexar is unavailable

**Deliverables**:
- [ ] Update capture_device_logs_sftp() to use Lexar path
- [ ] Create execution folder structure
- [ ] Update tar command to use correct remote path
- [ ] Add permissions check and fallback logic
- [ ] Test with actual Lexar USB mount
- [ ] Update documentation with new path structure

**Success Metrics**:
- Logs stored in `/media/lrqa/Lexar/` directory
- Folder structure organized by job_id and device
- All existing functionality works with new path
- Permission errors handled gracefully

---

## 24. Multi-User Database Access Control (Phase 2 - Enterprise Feature)
Enable all users to have controlled database access through application authentication without manual DB setup.

**Status**: 🔶 PHASE 2 (Planning - Target: August 2, 2026)

**Requirements**:
- Admin API to create application-authenticated DB users
- Team-based role assignment and permissions
- Single sign-on via application credentials (NTID/email)
- Automatic DB user provisioning on first login
- Role-based database access control
- Audit trail for all access and permission changes
- Support bulk import of team members from company directory

**Implementation Details**:
- Create DB user management API (controllers/db_user_controller.py)
- Add team-based role model (models/team_role.py)
- Implement application-level access control middleware
- Add audit logging for DB access events
- Create admin dashboard for user management

**API Endpoints**:
- `POST /api/admin/db-users` - Create new DB user
- `GET /api/admin/db-users` - List all DB users
- `PUT /api/admin/db-users/{user_id}` - Update user role
- `DELETE /api/admin/db-users/{user_id}` - Revoke DB access
- `POST /api/admin/teams/{team_id}/members/bulk-import` - Import NTIDs

**Data Model**:
```
TeamRole {
  role_id: str
  team_name: str
  can_create_devices: bool
  can_run_tests: bool
  can_view_results: bool
  can_manage_team_users: bool
  can_create_sequences: bool
}
```

**Deliverables**:
- [ ] Create DB user management API
- [ ] Implement team role model
- [ ] Add access control middleware
- [ ] Create admin dashboard for user management
- [ ] Add bulk NTID import functionality
- [ ] Audit logging for all access events
- [ ] Create documentation and user guide

**Success Metrics**:
- All team members can login with NTID/email
- Database access granted automatically
- Role-based permissions enforced
- No manual DB setup required
- Complete audit trail maintained

---

## 25. Multi-Team Deployment Management (Phase 2 - Enterprise Feature)
Enable multi-team deployments with admin-controlled team creation, NTID management, and team-specific access controls.

**Status**: 🔶 PHASE 2 (Planning - Target: August 5, 2026)

**Requirements**:
- Admin interface to create and manage teams
- Associate NTIDs and emails with teams
- Team-specific device assignments
- Team-based data isolation (devices, sequences, results)
- Bulk import of team members from company directory
- Team hierarchy support (main team, sub-teams)
- Cross-team restricted access (admins only)
- Location/region tagging for teams

**Implementation Details**:
- Create Team model (models/team.py)
- Create Team management controller (controllers/team_controller.py)
- Build admin dashboard for team management (templates/team_management.html)
- Implement team-based access control filters
- Add team membership validation on all operations

**Team Model**:
```
Team {
  team_id: str
  team_name: str
  description: str
  location/region: str
  ntid_list: List[str]
  members: List[User]
  devices: List[Device]
  sequences: List[SavedSequence]
  created_by: str
  created_at: datetime
  is_active: bool
}
```

**API Endpoints**:
- `GET /api/teams` - List all teams (admin only)
- `POST /api/teams` - Create new team
- `PUT /api/teams/{team_id}` - Update team
- `DELETE /api/teams/{team_id}` - Delete team (archive only)
- `POST /api/teams/{team_id}/members` - Add team members
- `DELETE /api/teams/{team_id}/members/{ntid}` - Remove team member
- `POST /api/teams/{team_id}/devices` - Assign device to team
- `GET /api/teams/{team_id}/devices` - Get team devices

**UI Components**:
- Team creation modal
- Team member management interface
- Bulk NTID import from company directory
- Team device assignment panel
- Team device filter in main UI

**Access Control Rules**:
- Users only see devices assigned to their team
- Users only see sequences shared with their team
- Users only see execution results for their team devices
- Admins can view/manage all teams
- Device access restricted to device's team

**Deliverables**:
- [ ] Create Team model and database schema
- [ ] Create Team management controller
- [ ] Build team administration UI
- [ ] Implement team-based access control filters
- [ ] Add bulk NTID import functionality
- [ ] Create team device assignment logic
- [ ] Add team-based result filtering
- [ ] Documentation and user guide

**Success Metrics**:
- Multiple teams can coexist in single deployment
- Each team has isolated device and data access
- Team members added via NTID import
- Admins manage all teams from single interface
- No cross-team data leakage

---

## 26. Multi-Team Team Admin System (Phase 3 - Enterprise)
Implement role-based team administration with super admin and team admin roles for full multi-team deployment and control.

**Status**: ✅ IMPLEMENTED (July 20, 2026)

**Features Implemented**:
- ✅ Super Admin Role (`is_super_admin` field in User model)
  - Controls entire application
  - Can create teams, manage all users
  - Can force delete any sequence regardless of creator
  - Can access all team resources
  - vpatne290 configured as super administrator

- ✅ Team Admin Role (`is_team_admin` field in User model)
  - Controls own team only
  - Can manage team members (add, update, remove)
  - Can delete sequences created by any team member
  - Cannot access other teams' resources
  - Limited to team-level operations only

- ✅ Team Management API (controllers/team_controller.py - 6 methods):
  - `list_teams()` - Super admin views all teams
  - `create_team()` - Create new team with bulk member import
  - `add_team_member()` - Add or update user in team
  - `update_team_member()` - Change member role or team assignment
  - `delete_team_member()` - Remove user from team (protects super admin)
  - `get_team_stats()` - Team statistics (member count, admin count, etc.)

- ✅ Team Management UI (templates/team_management.html):
  - Create new team form with description
  - Bulk member import fields: NTID, Email, Name, Team Admin checkbox
  - Dynamic form fields (add/remove members)
  - Teams list with statistics and member display
  - Member role badges (✅ Team Admin highlighted)
  - Real-time API integration with error handling
  - Responsive Bootstrap 5 design

- ✅ Enhanced Authorization:
  - DELETE /api/sequences endpoint enforces:
    - Creator can delete own sequences
    - Super admin can delete any sequence
    - Team admin can delete team member sequences
    - Comprehensive audit logging on all operations
  - All privileged operations verified with role checks
  - 403 Forbidden responses with detailed error messages

- ✅ API Endpoints (6 routes in app.py):
  - `GET /api/teams` - List teams (super admin only)
  - `POST /api/teams` - Create team with bulk member import
  - `POST /api/teams/members` - Add user to team
  - `PUT /api/teams/members` - Update member role
  - `DELETE /api/teams/members` - Remove user from team
  - `GET /api/teams/stats` - Team statistics
  - `GET /admin/teams` - Render team management interface

- ✅ Enhanced Auth Status Endpoint:
  - Returns `is_super_admin`, `is_team_admin`, `team_name` with user info
  - Frontend can detect user role and show appropriate UI

- ✅ User Model Updates (models/user.py):
  - Added `is_super_admin` parameter (default False)
  - Added `is_team_admin` parameter (default False)
  - Updated `to_dict()` to serialize both role fields
  - Updated `from_dict()` to deserialize with fallback to legacy `is_admin`
  - Maintains backward compatibility with existing `is_admin` field

- ✅ Data Configuration (Json/users.json):
  - `vpatne290` marked as super admin with `is_super_admin: true`
  - `vpatne290` marked as team admin with `is_team_admin: true`
  - Retains `is_admin: true` for backward compatibility

- ✅ User Interface Integration:
  - Navbar link "🔐 Teams" for super admin users
  - Only visible to super admin (conditional rendering)
  - Links to /admin/teams team management dashboard

**Data Model**:
```
User (Enhanced):
  is_super_admin: bool = False          # Controls entire application
  is_team_admin: bool = False           # Controls own team only
  team_name: str = ''                   # Team assignment
  is_admin: bool = False                # Legacy field (deprecated)

API Response (Team Create):
  {
    "success": bool,
    "team_name": str,
    "members_created": int,
    "created_members": List[{
      "ntid": str,
      "email": str,
      "name": str,
      "is_team_admin": bool,
      "temporary_password": str
    }],
    "timestamp": UTC_datetime
  }

API Response (Delete Sequence):
  {
    "success": bool,
    "error": str or None,              # Detailed error if unauthorized
    "sequence_id": str,
    "timestamp": UTC_datetime
  }
```

**Implementation Files Modified**:
1. `models/user.py` - Added role fields and serialization
2. `controllers/team_controller.py` - Created (300+ lines, fully functional)
3. `templates/team_management.html` - Created (400+ lines, responsive UI)
4. `templates/index.html` - Added navbar link for super admin
5. `app.py` - Added 6 API endpoints + enhanced /api/auth/status + enhanced DELETE authorization

**Access Control Rules**:
- Super admin: Full access to all application features
- Team admin: Can manage own team, delete own team's sequences
- Regular user: Can only manage own sequences
- All privileged operations logged to stderr with timestamps

**Audit Logging**:
- Every team operation logged: creation, member add/update/delete
- Every DELETE operation logs: who deleted, authorization check result
- All logs include: timestamp (UTC), user, action, success/error
- Audit trail maintained for compliance and security review

**Error Handling**:
- 403 Forbidden: Detailed error explaining why user cannot access
- Validation errors: Structured JSON response with field errors
- User creation: Temporary password generated, email delivery TBD
- Member protection: Super admin (vpatne290) cannot be deleted or demoted

**Deliverables**:
- ✅ Super admin role implementation
- ✅ Team admin role implementation
- ✅ Team management controller (6 methods)
- ✅ Team management UI with forms
- ✅ API endpoints for all operations
- ✅ Enhanced authorization on DELETE
- ✅ Audit logging on all operations
- ✅ Navbar integration
- ✅ User model serialization
- ✅ Backward compatibility maintained

**Success Metrics**:
- ✅ vpatne290 can access team management page
- ✅ vpatne290 can create teams and bulk import members
- ✅ vpatne290 can manage team members (add, update, remove)
- ✅ Team admins can delete sequences from own team only
- ✅ Super admin can delete any sequence
- ✅ Regular users restricted to own sequences
- ✅ All operations properly authorized
- ✅ Comprehensive audit trail maintained
- ✅ No cross-team data leakage
- ✅ Production-ready error handling

---

## Phase 27 - Database Persistence & Audit Logging (July 20, 2026)

**Objective**: Migrate Job execution data from JSON-only storage to PostgreSQL database with comprehensive audit logging and transaction safety.

**Scope**:
- Migrate Job model to PostgreSQL persistence
- Implement transaction rollback on database errors
- Create audit logging service for all CRUD operations
- Data versioning and point-in-time reconstruction
- Dual persistence pattern (PostgreSQL primary + JSON backup)

**Implementation Details**:
- **Job Model Migration**: Load from DB first with JSON fallback
- **Transaction Safety**: TransactionRollbackHandler with automatic rollback on errors
- **Audit Logging**: AuditLoggingService with 4 comprehensive methods
- **Data Versioning**: Reconstruct entity state at any point in time
- **Impact Assessment**: Risk-based analysis (CRITICAL/HIGH/MEDIUM/LOW)
- **Connection Pooling**: 20 connections, 40 max overflow for scalability

**Key Files Modified**:
- `models/job.py`: Added DB-first loading, dual persistence, transaction handling
- `services/audit_logging_service.py`: NEW - 380+ lines with 3 service classes

**Key Classes Created**:
1. **AuditLoggingService**: Log CRUD operations with automatic user detection
2. **TransactionRollbackHandler**: Execute operations with automatic rollback
3. **DataVersioningService**: Query entity history and reconstruct past states

**Deliverables**:
- ✅ Job model fully migrated to PostgreSQL
- ✅ Dual persistence (DB + JSON) implemented
- ✅ Transaction rollback handler created
- ✅ Audit logging service (380+ lines, 3 classes)
- ✅ Data versioning service operational
- ✅ Connection pooling configured
- ✅ Circular import issues resolved (deferred imports)
- ✅ Comprehensive stderr logging implemented
- ✅ Flask app deployed and tested
- ✅ Zero data loss architecture verified

**Success Metrics**:
- ✅ All job CRUD operations persist to PostgreSQL
- ✅ JSON backup always kept in sync
- ✅ Database errors trigger automatic rollback
- ✅ Complete audit trail for all operations
- ✅ Entity history queryable and searchable
- ✅ No circular dependencies in code
- ✅ App startup without errors
- ✅ Graceful degradation if DB unavailable

---

## Phase 28 - Change Password Feature (July 20, 2026)

**Objective**: Implement secure change password functionality for all logged-in users with OTP verification and current password validation.

**Scope**:
- Add "Change Password" button to dashboard navbar
- Three-step password change flow with security validation
- Current password verification (werkzeug hash)
- Email-based OTP verification (6-digit code)
- New password update with strength requirements
- Dual persistence (PostgreSQL + JSON)

**Implementation Details**:
- **Step 1**: User enters current password → OTP sent to email
- **Step 2**: User enters 6-digit OTP from email → Code validated
- **Step 3**: User sets new password → Validates and persists to DB + JSON

**Key Files Created**:
1. `templates/change_password.html`: Current password input form (169 lines)
2. `templates/verify_change_password_code.html`: OTP verification form (175 lines)
3. `templates/update_password.html`: New password form with strength indicator (290 lines)

**Files Modified**:
- `app.py`: Added 3 routes (180+ lines)
  - `/change-password` (GET/POST): Current password validation + OTP send
  - `/verify-change-password-code` (GET/POST): OTP verification
  - `/update-password` (GET/POST): Password update with validation
- `templates/index.html`: Added "Change Password" button to navbar

**Deliverables**:
- ✅ Current password validation (werkzeug hash verification)
- ✅ 6-digit OTP generation and email delivery
- ✅ OTP validation with 10-minute expiration
- ✅ Session-based security flow (NTID matching)
- ✅ Password strength requirements (8+ chars, different from current)
- ✅ Real-time password match validation
- ✅ Dual persistence (PostgreSQL + JSON)
- ✅ Error handling with user-friendly messages
- ✅ Responsive bootstrap design
- ✅ Accessibility features (password visibility toggles)
- ✅ Dashboard navbar integration
- ✅ @login_required protection on all routes

**Success Metrics**:
- ✅ Change Password button visible in navbar after login
- ✅ Current password validation working
- ✅ OTP email sent and received
- ✅ OTP verification prevents invalid codes
- ✅ Time-based OTP expiration enforced
- ✅ Password strength requirements enforced
- ✅ Password match validation working
- ✅ Old password rejected after change
- ✅ New password works for login
- ✅ Session cleared after successful change
- ✅ Redirect to login after password change
- ✅ No data loss (dual persistence)

---

## Implementation Timeline

**Phase 1 - Quick Wins** (Target: July 22, 2026 - 1-2 days):
- ✅ [20] Device Selection Auto-Refresh
- ✅ [21] Device Execution Status with Username
- ✅ [22] Custom Log Filtering (Reboot v2)
- ✅ [23] Log Storage Path Optimization

**Phase 2 - Enterprise Features** (Target: August 5, 2026 - 5-7 days):
- 🔶 [24] Multi-User Database Access (Planning)
- 🔶 [25] Multi-Team Deployment (Planning)

**Phase 3 - Multi-Team Deployment** (Target: August 10, 2026):
- ✅ [26] Multi-Team Team Admin System (COMPLETED - July 20, 2026)

**Phase 4 - Database Persistence & Security** (Target: July 20, 2026):
- ✅ [27] Database Persistence & Audit Logging (COMPLETED - July 20, 2026)
- ✅ [28] Change Password Feature (COMPLETED - July 20, 2026)

---

## Tracking & Monitoring

All new phases (20-28) will be tracked in:
- `/memories/repo/v2-implementation-status.md` - Phase completion tracking
- `/memories/repo/v2-work-progress.md` - Daily progress updates
- `/memories/repo/v2-blockers-and-issues.md` - Issues and solutions
- Git commit history with phase tags: `phase-20`, `phase-21`, etc.



